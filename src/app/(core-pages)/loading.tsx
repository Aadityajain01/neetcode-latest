// No route-group level loading skeleton.
// Each page handles its own loading state internally
// to avoid a double-skeleton flash (generic → page-specific → content).
export default function Loading() {
  return null;
}
